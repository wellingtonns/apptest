apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-test
  namespace: apptest
spec:
  revisionhistoryLimit: 2
  replicas: 2
  selector:
    matchLabels:
      app: app-test
  template:
    metadata:
      labels:
        app: app-test
    spec:
      containers:
        - name: app-test
          image: welignton/apptest:9e6ea98
          ports:
            - containerPort: 9000

